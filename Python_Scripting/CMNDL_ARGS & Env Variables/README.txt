# when the input is given in CMD it is called as command line arguments
# To read parameters from the command line arguments 'sys' is the module used.

sys is not pypi module above said is just for example. 'sys' will be there by default during python installation.

$ python_calculator.py 2 add 3 

sys.argv[1]   // This will input 2 from above CMD line arguments
sys.argv[2]   // This will input 3 from above CMD line arguments
sys.argv[3]